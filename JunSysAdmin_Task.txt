1. Да се извадят всички IP адреси, с отворили връзки към порт 80 със статус ESTABLISHED. Да се подредят по брой и да се сортират в обратен ред.

2. Да се блокират топ 5 IP адреси от резултата в задача 1 директно в iptables, като се използва for цикъл.

3. От access лога на apache, да се извадят всички адреси достъпвали машината за определена дата, да се подредят по брой и да се сортират в обратен ред.

4. Да се извади Модел и Сериен номер на определен хард диск, като се използва команда само на 1 ред в конзолата.

5. В OpenVZ хардуерен нод - да се извадят всички VPS-и, които са в статус running и да се подредят в обратен ред по размер на използвано CPU към момента.
Тук не е нужно да имате под ръка OpenVZ нод, просто трябва да намерите как се вади този списък и да го сортирате.

6. С tcpdump да се проследят всички заявки към порт 80, да се запишат във файл, след което да се извадят топ 5 най-посещавани сайта и да се подредени в обратен ред.

7. Да се напише команда, с която се прехвърля една база данни от един сървър на друг, като mysql-a на втория не слуша на порт 3306.

8. Да се намерят и изтрият всички файлове завършващи с “tar.gz”, с максимална дълбочина 3 директории от текущата.
Трябва да се направи с команда на един ред в конзолата.

9. Имаме следната ситуация: на mysql server цялата конзола зависва, сървърът ще забие. Как ще спрете mysqld:
	a) kill -9 <pid>
	b) killall mysqld
	c) /etc/init.d/mysqld stop 
Аргументирайте отговора си.

10. Ако имате флууд от 216.25.50.11 и 216.25.52.10 коя мрежа ще блокирате?
- 216.0.0.0/8
- 216.0.0.0/16
- 216.0.0.0/24